
# md -> latex
- map frontmatter title to cv job title
- map the rest of frontmatter items

fix languages section (try to use moderncv's class)

# errors 
can't have multiple paragraphs within cventry's last parameter. current workaround: just removing double newline, but then the paragraphs merge into one  